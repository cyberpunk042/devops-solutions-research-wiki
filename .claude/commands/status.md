Show the current state of the research wiki.

1. Run `python3 -m tools.pipeline status` for raw/wiki counts
2. Run `python3 -m tools.stats` for detailed stats (types, domains, layers, maturity)
3. Run `python3 -m tools.setup --services` for service status
4. Report everything in a clean summary
