# User Directive — 2026-04-08 — Ecosystem Connections & Automation Vision

## Verbatim

> lets make connections with things we use too. claude code in general and all its features, and codes and localAI and other claude plugins and tools and openclaw and openarms and openclaw-mission-control, and lightrag and plane and openfleet. and their multiple deserved documents fore each.
>
> I should be able to add a list of things to research online and/or local in order to automate my needs, however many pipelines or pipelines options we need.
> And we need to automate what can be automated and group them and make sequence / chain and group call / trees operations in order to always move toward the targets and offload as much as possible the repeatitive task or even enhance or encapsulate one.

## Interpretation

### Scope expansion — two dimensions:

**1. Ecosystem documentation:**
The wiki should document the user's actual toolchain, not just external sources:
- Claude Code (features, commands, hooks, plugins, MCP)
- LocalAI (backends, models, routing, RAG)
- OpenClaw + OpenArms + Mission Control (agent framework)
- LightRAG (graph DB, entity extraction, query modes)
- Plane (project management)
- openfleet (10-agent workforce, orchestration)
- Each tool deserves its own set of wiki pages

Sources: local project files (../openfleet/, ../devops-expert-local-ai/, etc.) + online docs

**2. Pipeline automation:**
- User should be able to submit a research list (URLs, topics, local paths)
- System processes them through automated pipelines
- Pipelines should support: sequencing, chaining, grouping, tree operations
- Goal: offload repetitive tasks, encapsulate operations, always move toward targets
- Multiple pipeline types: online research, local ingestion, cross-referencing, deepening
